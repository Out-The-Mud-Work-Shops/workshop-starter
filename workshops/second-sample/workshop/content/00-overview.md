# Second Sample

This is a second workshop in the same repo. Its only job is to prove that one
co-creator repo can hold many workshops: the OTM reconciler discovers every
`workshops/*/` folder and deploys each as its own catalog entry.

```terminal:execute
command: echo "second-sample is live in $SESSION_NAME"
```

Click **Next** to finish.
